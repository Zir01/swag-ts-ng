"use strict";
var expect = require('chai').expect;
const testHelper = require("./samples/testHelper");
describe("interfaceCreator", () => {
    describe("create()", () => {
        it("should create an array of valid interface definitions", () => {
            var swaggerObject = testHelper.getSampleSwaggerObject();
        });
    });
});

//# sourceMappingURL=interfaceCreatorSpec.js.map
