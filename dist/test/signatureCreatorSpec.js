"use strict";
var expect = require('chai').expect;
const testHelper = require("./samples/testHelper");
describe("signatureCreator", () => {
    describe("create()", () => {
        it("should create an array of valid signature definitions", () => {
            var swaggerObject = testHelper.getSampleSwaggerObject();
        });
    });
});

//# sourceMappingURL=signatureCreatorSpec.js.map
